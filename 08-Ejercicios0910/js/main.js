// 1. Dado un string de palabras, devolver la palabra más larga.

//let message = prompt("Enter your message here:");
//console.log(message.length);

//2.Crear un programa que permita detectar si una frase finaliza con punto.
//let puntoMessage = prompt("Enter your message here:");
//puntoMessage = /[.]/gi;

function pyramyd() {
  let rows = 10;
}
